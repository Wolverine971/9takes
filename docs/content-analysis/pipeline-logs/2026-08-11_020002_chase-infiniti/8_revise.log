Revised `src/blog/people/drafts/Chase-Infiniti.md`.

**Triggered by:** grade B (8.2) with Originality capped at 7 by the cross-draft sameness check, plus `verification-initial.md` returning `verification_status: fail` with 2 open P0s. Lint was already clean going in.

**Perspective P0s (both closed):**
- **P0-01** — the Kulukundis "so special" quotation was two segments of one answer spliced into a continuous three-sentence quote, no ellipsis, with `she just felt` recased to hide the seam. Took the verifier's second option (separate attribution) rather than the ellipsis, because it also fixes the framing defect they named in the same paragraph.
- **P0-04** — the verifier marked this needs-human. I took the conservative option (a): remove the assertion that Kulukundis's screen-test account and Infiniti's Kimmel account describe one session, without adjudicating which label is right. It turned out to live in **four** reader-visible slots, not the one quoted: cold open, frontmatter `description`, FAQ 3, and FAQ 1 + the TL;DR bullet. All four fixed; `grep "confirmed it|confirmed the guess"` returns zero.

**Grader items fixed:** the Nine-arrow contradiction (arrow mechanics and the Type 9 link out of the body, so it stops claiming what the rabbit hole retracts); awards ledger pinned inline to *Variety* Jan 2026 and the *Variety*-reported NBR sweep Dec 2025; imperatives cut 5→2 exactly as asked; five more two-beat pairs removed; all three stale ledgers recounted — DISTRIBUTION was genuinely wrong at 3 and is now 4, at the gate limit.

I also **cut "The trades called it a surprise"** rather than sourcing it. Three searches found no trade that said it, and inventing an outlet on a load-bearing Emmy claim is worse than losing the line.

**Three refusals, all flagged rather than silent:**
- "Ambition is the boring part" is **PROTECT-07**; the close's razor callback is **PROTECT-08**. The grader asked to cut both. A revision pass doesn't regress protected hits — and the closing-shape problem is corpus-level (fourth draft in a row), better solved at creator stage on the next subject. This means the sameness cap may not fully clear.
- The Indianapolis trim is **partial**: ~49 words of résumé out, but the parking-quote scene the grader also asked for cost about the same, so the section is net flat (543→542). The remaining 150 would mean deleting the Summer Stock Stage run (the P0-05 repair for fabricated credits), *Presumed Innocent*, the dated Tyler/Ateez beats (P1-09), or the Zendaya consequence. That's a human's call, not mine.

All twelve protected hits re-verified — the master-class table diffs byte-identical against the snapshot.

**Lint: 0 fail, 1 warn** (4,487 words vs 4,500 ceiling — improved from 4,499; description 155 chars, now inside target).

Artifacts: `revision-resolution.md` in the review dir, and a `REVISION PASS NOTES` block on the draft. One thing worth your attention beyond this pass: the verifier found the AOL copy of the Kimmel article now 404s while the Yahoo copy resolves — that's the URL carrying the P0-04 repair, so **P1-14 archiving is urgent before publish**. The pipeline will clear the consumed review sidecar and re-grade; targeted perspective verification must rerun, since this pass doesn't declare the gate passed.
